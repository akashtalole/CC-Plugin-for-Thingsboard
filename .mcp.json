{
  "mcpServers": {
    "thingsboard": {
      "command": "npx",
      "args": ["-y", "thingsboard-mcp"],
      "env": {
        "TB_BASE_URL": "${TB_URL}",
        "TB_USERNAME": "${TB_USERNAME}",
        "TB_PASSWORD": "${TB_PASSWORD}"
      }
    }
  }
}
